# cAdvisor on Homeassistant

## Build

The `Dokerfile` will clone the current cAdvisor repository and build the executable on the fly. This is fine most of the time, until somthing is broken at the cAdvisor repository.
